<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/publisher_layout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/video_clickable_foreground_selector"
                android:clickable="false"
                android:paddingBottom="2dp"
                android:paddingLeft="13dp"
                android:paddingTop="2dp">

    <com.example.admin.miniproject.view.CircleImageView
        android:id="@+id/publisher_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/ldh"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="5dp"
        android:layout_toRightOf="@+id/publisher_icon"
        android:orientation="vertical">
           <TextView
                android:id="@+id/publisher_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginRight="4dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:maxWidth="135dp"
                android:text="刘德华"
                android:textColor="#3b424c"></TextView>
    </LinearLayout>

</RelativeLayout>